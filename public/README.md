# Public Assets Directory
Place harvested X screenshots inside `screenshots/` and downloaded MP4 clips inside `clips/`.
When user drops `voiceover.mp3`, place it directly here in `public/voiceover.mp3`.
